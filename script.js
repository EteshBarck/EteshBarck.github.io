// script.js
document.addEventListener('DOMContentLoaded', () => {
  // Burada at listesi ve yarış simülasyonu fonksiyonları bulunacak

  // At listesi örnek verileri
  const horses = [
    { name: 'Black Beauty', performance: 'WWLL', kg: '500', age: '4' },
    // ... Diğer atlar
  ];

  // At listesini doldur
  function populateRaceList() {
    // Tabloyu dolduracak fonksiyon
  }

  // At yarışı simülasyonu
  function simulateRace() {
    // Yarış simülasyonunu yapacak fonksiyon
  }

  // Kutlama mesajı göster
  function showCelebration() {
    // Kutlama mesajını gösterecek fonksiyon
  }

  // Fonksiyonları burada çağır
  populateRaceList();
  // ... Diğer fonksiyonlar
});
